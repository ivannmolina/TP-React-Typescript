import { Spinner } from "react-bootstrap"

const Loader = () => {
  return (
    <div className="Loader">
        <Spinner animation="border" className="loader-spinner" variant="light" >
            
        </Spinner>
    </div>
    
  )
}

export default Loader